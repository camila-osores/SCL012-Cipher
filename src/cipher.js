window.cipher = {

  encode: () => {
    /* Acá va tu código */
  //   cifrar.addEventListener("click", () => {

  //     let offset = document.getElementById("desplazamiento").value;

  //     let arr = [];

  //     for (let i = 0; i < pass1.value.length; i++) {

  //         ascii = pass1.value.charCodeAt(i);

  //         aum_arr = (ascii - 65 + parseInt(offset)) % 26 + 65;

  //         arr.push(String.fromCharCode(aum_arr));
  //     }

  //     pass2.textContent = `Tu clave cifrada es : ${arr.join("")} y recuerda que tu desplazamiento para decifrar es : ${offset}`
  // })
    
  },
  
  decode: () => {
    /* Acá va tu código */
  }
};
